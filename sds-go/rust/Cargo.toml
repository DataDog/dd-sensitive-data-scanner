[package]
name = "sds_go"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
crate_type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dd-sds = { path = "../../sds" }
sds-bindings-utils = { path = "../../sds-bindings-utils" }

